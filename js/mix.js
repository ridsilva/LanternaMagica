
  /*
  // Second canvas
  let sketch2 = function(p) {
    p.setup = function() {
      p.createCanvas(200, 200).parent('canvasMixContainer');
    };

    p.draw = function() {
      p.background(0, 0, 255);
      p.rect(p.width / 2 - 25, p.height / 2 - 25, 50, 50);
    };
  };

  // Create instances of p5
  new p5(sketch2);


  */